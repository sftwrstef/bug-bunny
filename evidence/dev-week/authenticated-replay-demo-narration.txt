Bug Bunny is a local-first proof console for authorized web research. It starts with a reusable policy boundary for a reviewed HackerOne, Bugcrowd, or Intigriti program. This recording contacts none of them; it switches to a localhost fixture with two synthetic accounts and a four-request ceiling.

In Proof Lab, I paste each account's DevTools cURL capture. Tokens, object IDs, URLs, markers, and bodies stay ephemeral: never rendered or saved.

Before traffic, the preview confirms one endpoint shape, one changed locator, and two distinct sessions. I add harmless owner-only markers and confirm control.

The matrix runs B-to-B, A-to-A, then derives B-to-A by changing only the session. A's marker appears under B, so Bug Bunny stops immediately at three of four requests.

The write-once receipt keeps hashes, marker booleans, timings, network controls, and integrity metadata. Kimi can challenge it separately; this recording stops at the deterministic result, with human review still required.

Codex with GPT-5.6 Sol turned the hard-coded IDOR demo into a reusable structural cURL parser, ephemeral-secret boundary, and four-request control-and-verdict gate, then added secure, failed-control, redirect, and tamper fixtures and tests. Bug Bunny turns a hunch into bounded, reproducible proof.
